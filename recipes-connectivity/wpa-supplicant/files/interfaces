auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp

#auto wlan0
iface wlan0 inet dhcp
	pre-up cp /etc/wpa_supplicant.conf /data
        wireless_mode managed
        wpa-conf /data/wpa_supplicant.conf
